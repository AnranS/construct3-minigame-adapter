# 验证记录与兼容边界

记录日期：**2026-09-20**。工具版本：**0.2.0**。当前功能工程：**MiniGameApiSuite**。

当前已经完成真实 Construct 编辑器导出、微信与抖音转换构建，以及微信 IDE 的启动、渲染、17 项自检和部分交互测试。**尚未完成抖音 IDE、两端真机及全部平台业务 API 的验收。** 微信 IDE 的 Modal Worker 错误仍未修复。

## 这些数字分别代表什么

| 数字 | 具体含义 |
| --- | --- |
| **171 个 API 名称 / 20 类** | 适配层显式调用目录，包含微信 142 个名称、抖音 140 个名称；不表示逐项实测通过 |
| **133 / 171** | 当前微信 IDE 宿主的能力探测结果，表示方法可用；不代表已授权、配置正确或业务调用成功 |
| **51 个入口 / 11 类** | MiniGameApiSuite 功能页面的组织方式；入口可组合多个 API |
| **214 项通过 / 0 失败** | 当前本地自动化回归（含原始引擎附加测试）；公开 CI 对应 213 项通过、1 项缺少本地引擎文件而明确跳过 |
| **17 成功 / 0 失败 / 0 跳过** | 当前示例在真实微信 IDE 中执行的自检结果，已从运行时控制台读回 |

目录、检测、自动化测试和真实设备验收是不同层级。当前不能宣称完整 Construct runtime、任意 Construct 游戏或全部 171 个 API 已适配并验收。

## 真实导出与运行环境

| 项目 | 本轮记录 |
| --- | --- |
| Construct | r495.2，真实编辑器打开、保存 MiniGameApiSuite 并执行 HTML5 导出 |
| 插件 | MiniGameBridge 0.2.0，Construct manifest 版本 0.2.0.0 |
| 原始导出 | 本地验证保留了 28 个文件的 HTML5 ZIP，内容未修改；转换补丁在构建阶段应用。公开仓库提供 `.c3p`，HTML5 需自行通过 Construct 导出 |
| 转换结果 | 微信、抖音工程均构建成功 |
| 微信 IDE | macOS，RC 2.02.2607161，实际运行基础库 3.15.3 |
| 渲染与就绪 | WebGL 2；收到 `[C3 MiniGame] Construct runtime-ready` |
| 设备配置 | iPhone 12 / iPad Pro 12.9 模拟器，分别完整重开项目后显示正常 |
| 启动日志 | 本轮新启动观察窗口内 0 条 error、2 条 warning；不包含后续 Modal 操作 |

模拟器设备配置不代表对应手机或平板真机。两端构建成功也不代表两端都已运行。

## 微信 IDE 自检

17 项自检运行了实际平台调用并检查结果，范围为：

- 设备、窗口、安全区、应用基础信息、启动参数、进入参数、系统设置。
- 网络类型。
- 示例专用原生存储键的写入、存储信息查询、删除。
- 临时文件写入、读取与清理的往返验证。
- 包内资源的 fetch、XHR 读取。
- 电量、亮度、性能时钟。

结果为 **17 成功、0 失败、0 跳过**。自检会操作示例专用存储和临时文件，不主动申请权限、分享或连接外部网络。

## 微信 IDE 交互实测

| 功能 | 实际观察 | 范围说明 |
| --- | --- | --- |
| Toast | 原生提示显示，收到成功回调 | 已观察本轮界面与回调 |
| Loading | 原生 Loading 显示，1 秒后 hideLoading 成功 | 已观察显示与隐藏 |
| ActionSheet | 选择第二项，返回对应选项 | 已观察选择与回调 |
| 短 / 长振动 | 两者均收到成功回调 | 未验证手机硬件振动 |
| Modal | 确认和 `cancel=true` 取消回调正常 | 同时仍出现 1 条 `worker path empty` 红色错误，未修复 |
| 原生键盘 | 显示输入框，输入 8 字符，收到 onKeyboardInput 与 onKeyboardComplete | 只记录长度；输入完成关闭后调用 hideKeyboard 成功，未验证该 API 主动隐藏仍打开的键盘 |
| 音频主动停止 | 8 秒音频播放期间主动 Stop，收到 onStop 并释放资源 | 已观察主动停止路径 |
| 音频自然结束 | 另一次完整播放后收到 onEnded | 已观察自然结束路径 |
| 分类与返回 | 功能分类和返回操作可用 | 不外推到全部输入设备和真机 |

启动无 error 不代表后续所有操作无 error。Modal 问题在基础库 3.15.3 和完整重开后的 3.12.1 均复现。本机只读调查定位为 IDE 转发内部 Worker URL 时丢失查询参数，但未获官方确认，也没有交付验证过的修复。复现步骤见[问题排查](../troubleshooting/)。

## 213 项自动化回归覆盖

最新执行 `npm test`：**213 项通过，0 项失败**。主要覆盖如下：

| 范围 | 检查内容 |
| --- | --- |
| 插件包与注册 | 官方 Schema、动作 / 条件 / 表达式一致性、语言条目、ZIP 根目录和打包内容 |
| 插件运行时 | 初始化、tag 隔离、JSON 与非 JSON 结果、取消、错误、实例清理和迟到回调 |
| 平台 API | 目录、平台差异、调用种类、成功 / 失败、超时、取消、事件 on/off 和能力缺失 |
| DOM 与资源 | 有限 DOM、Canvas 属性、全局隔离、DOMRect、Blob / URL、UTF-8、包内资源及 WASM 边界 |
| 网络 | fetch / XHR 失败、取消、超时、重入、旧响应污染；WebSocket 状态、消息顺序、二进制和关闭 |
| 音频与生命周期 | 播放、停止、换源、静音音量、等待结束、事件清理和释放异常 |
| 输入 | 触摸 ID、primary pointer、空 changedTouches 处理、滚轮事件、原生取消和订阅清理 |
| 存储 | 序列化、隔离、错误传播、已识别的 Construct localforage 接入 |
| 构建与导出协议 | 入口顺序、动态模块、内部任务消息、真实 ready 协议、失败构建保留旧产物、目录与依赖边界 |

这些测试主要使用受控的模拟 API，不连接真实账号后端、不展示真实广告、不执行发布。手写 Construct 协议夹具和原生 API 冒烟工程用于回归，不包含完整 Construct 引擎，也不是编辑器导出验收的替代品。

## 当前兼容边界

| 能力 | 当前实现与限制 |
| --- | --- |
| DOM / CSS | 只提供引擎启动和资源加载所需的有限接口，不实现完整浏览器布局、表单或任意 HTML 插件 |
| 渲染 | 使用宿主 Canvas / WebGL；本轮验证真实 Text 页面，其他插件、特效、着色器和性能需逐项目测试；未实现 WebGPU |
| Worker | 内部任务采用同线程异步兼容实现，无多线程加速、隔离或 transferable 分离语义 |
| 脚本 | 打包本地代码，拒绝远程可执行脚本、import map 和一般内联脚本 |
| 存储 | localStorage 与已识别的 runtime.storage 路径连接真实平台存储；不模拟完整 IndexedDB |
| fetch / XHR | 支持包内文件、data URL、原生 request；不提供响应流、同步 XHR、完整 cookie / credentials 管理 |
| WebSocket | 通过真实 SocketTask 桥接，保留连接与关闭状态；外部服务、域名、TLS、子协议仍需实际验证 |
| 音频 | 原生 InnerAudioContext 桥接；Web Audio 仅在宿主实际提供对应入口时暴露，完整效果链与解码路径有边界 |
| WASM | 使用真实宿主能力；微信仅接受包内路径或与已登记包内文件完全匹配的字节 |
| 平台业务 | 参数、权限、账号后端、广告位、开放数据域与平台设置由项目提供；支付不在当前目录 |

各接口名称和调用种类见[API 目录](../api/)。

## 尚待验收

- 抖音 IDE 的真实编译、初始化、首帧和平台调用。
- 微信与抖音真机的冷启动、渲染、触摸、音频、前后台切换、屏幕方向、性能及长时间运行。
- 最新滚轮实际转发、真机多点触摸、当前示例 onShow / onHide 和主动隐藏已打开键盘。
- 真实登录后端交换、生产网络请求、外部 WebSocket、真实广告完整观看及提前关闭。
- Modal Worker 错误在官方修正版本中的复测，以及真机是否受影响。
- 不同 Construct 版本、实际游戏插件、着色器、解码器和项目特定资源。

每个平台分别记录成功、失败和取消结果。用最小工程建立基线后，再加入游戏实际使用的功能，避免把某个示例通过外推为全部项目通过。

## 构建报告如何理解

转换器生成的 `BUILD-REPORT.json` 包含静态发现、入口和补丁记录，当前保留以下状态：

```json
{
  "validation": "build-only-not-device-verified",
  "deviceVerified": false,
  "workerExecution": "same-thread-asynchronous"
}
```

这是构建工具的状态声明，不覆盖本页单独记录的 IDE 实测证据。自己的项目完成真机验证后，应保存设备、版本、日志和复现步骤，不通过修改这一字段代替测试。开始接入请查看[快速开始](../guide/)。
